<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Foreground service (persistent background agent) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />

    <!-- Wake lock to keep the service alive -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Post notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Read device info -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Screen capture via MediaProjection -->
    <uses-permission android:name="android.permission.CAPTURE_VIDEO_OUTPUT" />

    <!-- Write to external storage for file transfers (Android ≤ 9) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".AgentApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_agent"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_agent"
        android:supportsRtl="true"
        android:theme="@style/Theme.SignalRemoteAgent">

        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Agent foreground service (persistent connection to server) -->
        <service
            android:name=".service.AgentForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|remoteMessaging" />

        <!-- Screen capture service -->
        <service
            android:name=".screen.ScreenCaptureService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- Accessibility service for injecting input events -->
        <service
            android:name=".service.InputInjectionService"
            android:exported="true"
            android:label="@string/input_injection_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
